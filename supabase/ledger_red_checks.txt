# Prod red set (crm-design mmkfpnshxjcyijhuydgr) captured before applying 0449
# on 2026-09-16. Distinct failing ledger_checks() names across all companies.
# Diff this list by NAME after the apply — 0449 must not turn any new check red.
#
# 0450 (contract_terms_are_gated_dated_and_the_split_is_operational) applied
# 2026-09-16: diffed clean — no new check went red. migration_ledger_matches_repo
# reads red only until the repo-manifest fetch picks up 0449/0450 after push.
#
# 0451 (ledger_cash_pay_rpc_fronted_tables_are_permission_gated) applied
# 2026-09-16: diffed clean — same 14 names, no new check went red.
#
# Money-movement pass (Stage A + Stage B), applied 2026-09-16, each diffed clean
# (same 14 names, no new red):
#   0452 salary_history_is_gated_on_pay_authority
#   0453 money_rpcs_definer_part1_apply_money_delta_and_expenses
#   0454 money_rpcs_definer_part2_invoices_transfer_inventory_partners
#   0455 apply_money_delta_is_internal_only
#
# 0457 (headcount_caps_count_per_category_across_shifts) applied 2026-09-17:
# diffed clean — same 14 names, no new check went red. Both slot triggers now
# cap per (contract, category, site) across shifts. NB: the recorded row
# `ggs_cash_in_hand_is_restated_to_the_counted_position` (20260916120447) that
# sat after 0455 with no repo file arrived via merge as 0456 — drift closed.
#
# Money-movement pass Stage C + Stage D (safe half), applied 2026-09-17, each
# diffed clean (same 14 names, no new red):
#   0458 disburse_payslip_moves_money_atomically — payslip CAS claim + money in
#        one SECURITY DEFINER tx (payroll.edit). Rolled-back 4-arm prod probe
#        passed: cash+bank move payslip+balance+GL together; a stale
#        p_expected_paid raises and moves nothing.
#   0459 treasury + payroll_runs perm_write gated (accounting.edit / payroll.edit)
#        — their only writers are now DEFINER RPCs. The other Stage-D tables
#        (payslips, cash_locations, cash_deposits, cheques, custody_transfers,
#        opening_balance_lines, fixed_assets) still have raw-write screens and
#        need an RPC-routing pass first (0451's recorded position) — REPORTED,
#        not gated. cash_locations especially: payroll/expenses write it.
alert_delivery_is_healthy
ar_control_equals_open_invoices
bank_control_equals_bank_accounts
bank_per_account_gl_equals_operational
cash_control_equals_cash_locations
cash_forecast_clears_the_floor
cash_per_location_gl_equals_operational
every_control_is_invoked
no_definer_function_crosses_a_branch
no_negative_custodian_balance
no_stale_prepaid_balance
payroll_accrual_matches_attendance
salaries_payable_equals_undisbursed_net_pay
